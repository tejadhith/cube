[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "twisty"
version = "0.1.5"
description = "Solve a cube under partial observability via MCP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.26.0",
    "magiccube>=1.2.0",
    "Pillow>=10.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0.0"]

[project.scripts]
twisty = "cube.server:main"

[tool.setuptools]
include-package-data = true
packages = ["cube"]
